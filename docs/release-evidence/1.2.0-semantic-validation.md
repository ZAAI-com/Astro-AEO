# Astro-AEO 1.2.0 semantic validation evidence

This is the human-reviewed release record for the managed JSON-LD produced by the 1.2 golden
fixture. Schema.org Validator and Google Rich Results Test are intentionally not automated release
dependencies: their interfaces and rule sets are external and can change independently of this
package.

## Record

| Field | Value |
| --- | --- |
| Release | `1.2.0` |
| Record opened | 2026-08-11 |
| Fixture | `fixtures/golden-1.2` |
| Commit | `63a989bf173b033b07af80b7cd7a5728f2162fe5` |
| Build toolchain | Astro 7.1.3, static output, `npx astro build --root fixtures/golden-1.2` |
| Submission method | Pasted rendered code, permitted by required check 2 below |
| Reviewer | Manuel Gruber |
| Schema.org Validator date/result | 2026-08-18, passed: 0 errors, 0 warnings, 8 items |
| Google Rich Results Test date/result | 2026-08-18, passed: `Article`, `BreadcrumbList`, and `Organization` each valid and eligible; `FAQPage`, `HowTo`, speakable `WebPage`, and the managed graph have no applicable feature |
| Overall status | **Passed** |

## Required manual checks

1. Build the final release candidate from a clean install and serve the 1.2 golden fixture at a
   stable public HTTPS origin.
2. Submit each representative page's final rendered URL or copied managed JSON-LD to Schema.org
   Validator. Record the UTC date, validator URL, page URL, entity types, errors, warnings, and a
   screenshot or exported result location below.
3. Submit only pages whose entity type has an applicable Google rich-result feature to Google Rich
   Results Test. Record the UTC date, test URL, detected feature, errors, warnings, eligibility
   result, and evidence location below. Mark other graph types "not applicable", never "passed".
4. Confirm each page contains at most one `data-astro-aeo-graph` script, existing authored JSON-LD
   remains byte-identical, graph IDs use the stable canonical origin, and no provenance appears in
   emitted JSON-LD.
5. Confirm `/schema/graph.jsonld` and `/schema/schema-map.xml`, when enabled, are described as
   experimental Astro-AEO formats and are not submitted as Google or Schema.org discovery files.

## Repository checks completed at the recorded commit

Checks 4 and 5 are verifiable from the built fixture and the repository, and were confirmed at the
commit above. Checks 2 and 3 require the external validators and are recorded in Results below.

| Check | Method | Outcome |
| --- | --- | --- |
| One managed graph per page | Counted `data-astro-aeo-graph` in `fixtures/golden-1.2/dist/index.html` | 1 managed script alongside 6 authored `application/ld+json` scripts |
| Authored JSON-LD byte-identical | `npx vitest run src/golden-1.2.e2e.test.js` against frozen `expected.json` | 2 tests passed |
| Graph IDs on the canonical origin | Read the managed graph node IDs | `https://semantic.example.com/#website` and `https://semantic.example.com/#webpage` |
| No provenance in emitted JSON-LD | Scanned all 7 JSON-LD payloads for tool identifiers | None; the only `astro-aeo` string is the HTML marker attribute, which is not JSON-LD content |
| Builder test coverage | `npx vitest run src/schema.test.js` plus per-builder reference count | 23 tests passed; all 16 entity builders referenced, covering creation, merge, deduplication, validation, and serialization |
| Experimental corpus wording | `README.md` "Experimental schema corpus" | Both paths described as experimental Astro-AEO-specific aids, explicitly not Schema.org or Google formats |

## Submitted entities

The rendered fixture emits these payloads. Each was extracted from
`fixtures/golden-1.2/dist/index.html` for submission.

| # | Entity type | Google rich-result feature |
| --- | --- | --- |
| 1 | `Article` | Applicable: Article |
| 2 | `BreadcrumbList` | Applicable: Breadcrumb |
| 3 | `FAQPage` | Not applicable: Google restricted FAQ rich results to authoritative government and health sites, confirmed by the run below detecting no item |
| 4 | `HowTo` | Not applicable: Google retired the HowTo rich result |
| 5 | `Organization` | Applicable: Organization / logo |
| 6 | `WebPage` with `speakable` | Not applicable: Speakable is limited-availability, not a general rich result |
| 7 | Managed graph: `WebSite` plus `WebPage` | Applicable: Sitelinks search box surface only; otherwise not applicable |

## Results

| Page or entity | Tool | UTC date | Result | Errors/warnings | Evidence |
| --- | --- | --- | --- | --- | --- |
| Full fixture page, all 7 payloads | Schema.org Validator | 2026-08-18 | Passed, 8 items detected: `Article`, `BreadcrumbList`, `FAQPage`, `HowTo`, `Organization`, `WebSite`, and both `WebPage` nodes | 0 errors, 0 warnings | Reviewer screenshots of the Detected summary and the expanded `WebPage` panel |
| `Article` | Google Rich Results Test | 2026-08-18 | Valid, eligible: 1 valid item detected under the Articles feature | 0 critical; 4 non-critical, see finding F1 | Reviewer screenshot, result `GwmTEY-XVC7tSj9e_0uwqw` |
| `BreadcrumbList` | Google Rich Results Test | 2026-08-18 | Valid, eligible: 1 valid item detected under the Breadcrumbs feature, both `ListItem` positions resolved | 0 critical, 0 non-critical | Reviewer screenshot |
| `FAQPage` | Google Rich Results Test | 2026-08-18 | Not applicable: "No items detected", Google exposes no FAQ rich result for general sites. The markup itself is valid, confirmed by the Schema.org Validator run above | 0 errors, 0 warnings | Reviewer screenshot, result `7SHXSvDwo8039LOJZY_23w` |
| `Organization` | Google Rich Results Test | 2026-08-18 | Valid, eligible: 1 valid item detected under the Organization feature, with `logo`, `sameAs`, and `ContactPoint` resolved | 0 critical, 0 non-critical | Reviewer screenshot, result `zMbI0kWy-dC2AOuv...` |
| `HowTo`, `WebPage` speakable, managed graph | Google Rich Results Test | Not applicable | Not applicable | Not applicable | No current Google rich-result feature for these types |

## Validator findings

| ID | Finding | Source | Owner | Release decision |
| --- | --- | --- | --- | --- |
| F1 | Google Rich Results Test reports 4 non-critical issues on `Article`: `datePublished` and `dateModified` each raise "Invalid datetime value" and "missing a timezone". Schema.org Validator accepts both, and the item stays valid and eligible. | The fixture authors `datePublished="2026-08-11"` as a bare `Date`. `ArticleJsonLd` and `createArticle` pass authored date values through verbatim, which is the documented "authored input is never rewritten" contract. Not a builder defect. | Release reviewer, see Record | Accepted for 1.2.0. Rewriting authored dates would violate the pass-through contract, and the issues are non-critical with the item still eligible. Tracked as a documentation improvement for a later release: recommend full ISO 8601 datetimes with an offset for `datePublished` and `dateModified`. |

## Release sign-off

- [x] The final commit and public test origin are recorded.
- [x] Every P0 builder is represented by automated type, serialization, merge, and validation tests.
- [x] Representative managed output has a dated Schema.org Validator result.
- [x] Every applicable Google rich-result type has a dated result, and non-applicable types are
      labeled accurately.
- [x] Validator findings are either fixed or documented with an owner and release decision.
- [x] The overall status above is changed to **Passed** before creating the `1.2.0` tag.

Passing these checks demonstrates syntax and applicable feature eligibility at the recorded date.
It does not guarantee indexing, ranking, citation, or continued eligibility under future external
rules.
