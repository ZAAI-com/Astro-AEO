# Astro-AEO 1.4.0 semantic validation evidence

Unlike 1.3.1, 1.4.0 changes JSON-LD-producing source, so nothing is carried forward. The 1.2 fixture was
resubmitted to both validators on 2026-09-22 and a new TechArticle fixture, covering the one new
public entity, was submitted alongside it. The results are recorded below. The external services are
manual release checks, not automated release dependencies.

## Record

| Field | Value |
| --- | --- |
| Release | `1.4.0` |
| Validation date (UTC) | 2026-09-22 |
| Fixture | `fixtures/golden-1.2` |
| Candidate commit | `b725be1` plus the commit that finalizes this record. Rerun the full gate there before tagging. |
| Build toolchain | Node 24.15.0, pnpm 11.17.0, Astro 7.3.1 |
| Submission method | Complete rendered HTML pasted into the Schema.org validator and Google's Rich Results Test (code input); no public deployment needed. Two documents: the rebuilt 1.2 fixture and a generated TechArticle page. |
| Submitted HTML SHA-256 | 1.2 fixture `eb9fae45e534ad8eea7b9523be0a564e389362a37455b0ca42325d556c33cf3c` (rebuilt from this tree, identical to the 1.3.0 submission). TechArticle page `93c19d03ff9df78be10281bd3c261f3f9c5681a358d5e4b37ce2487561e103b8`, generated with `createTechArticle()` from `src/schema.js`. |
| Operator | Claude, repository verification and local gate execution |
| Human reviewer | Pending: the release owner reviews the results below and signs off in this workspace conversation. |
| Overall status | Pending |

## Semantic source changed in 1.4.0

`git diff --name-only origin/main...HEAD` lists three paths under the JSON-LD surface.

| Path | Change | Reachable from the submitted fixture |
| --- | --- | --- |
| `src/schema.js` | Adds `createTechArticle()`, one more `buildEntity` wrapper. Renames a JSDoc-local typedef. No existing builder, merge, validation or serialization code changes. | No. The fixture uses no TechArticle. |
| `src/core/head.js` | The inferred site-wide `WebSite` entity omits `inLanguage` when the site declares more than one Astro i18n locale. Repeated `generator` meta tags are exempt from the duplicate-metadata diagnostic. | No. The fixture has one locale, so its `WebSite` bytes are unchanged; the second change affects a diagnostic, not output. |
| `components/index.d.ts` | Adds the optional `version` prop to the `AeoPage` declaration. | No. A type declaration, and `AeoPage` emits no JSON-LD. |

The byte-identical fixture hash above proves the first three rows for the submitted page. The
`TechArticle` entity, the one new public output, was submitted separately (results below). The
multilingual `WebSite` entity without `inLanguage` was not submitted: the change removes one optional
property from an entity type both validators already accepted, and it is pinned by `src/core/head.test.js`
and the `recipes/i18n` build.

## Repository checks

| Check | Method and result |
| --- | --- |
| Frozen outputs | `fixtures/golden-1.0` and `fixtures/golden-1.2` pass byte for byte in the default suite, as does `fixtures/config-compat`. |
| Builder coverage | `src/schema.test.js` covers all 18 builders, including `createTechArticle`; public consumer types pass on TypeScript 5.5. |
| Multilingual WebSite | `src/core/head.test.js` pins the entity with and without a second locale, and `recipes/i18n` builds, which it could not before the fix. |
| Full release gate | `pnpm run release:check` passed at `2e07672` in 140 seconds (log kept locally as `.context/release-1.4.0/full-gate-2e07672.log`): schema and migration-doc freshness, Changeset coverage, release metadata, both type checks, the default suite (1768 tests), development (29), SSR (50), the trailing-slash matrix, adapter builds (27) and runtimes on Node, workerd and Deno (108), the workerd edge suite (29), recipes (9), the 10,000-page audit benchmark, bundle baselines, packed-tarball install, the IndexNow transport smoke and performance ceilings. It must be rerun at the final candidate commit. |
| Performance comparison | Against the committed 1.3 baseline: two portable regressions, both package size and both explained in `CHANGELOG.md` (28.76 percent packed, 22.83 percent unpacked), no Node or Cloudflare bundle regression, no absolute ceiling failure. |
| Benchmark reference | A complete 1.4 reference (sizes, timing, memory, corpus, request and Worker startup samples) was recorded on 2026-09-22 with runner class `astro-aeo-m2-pro-reference` on an Apple M2 Pro, with no ceiling failure, and committed as `benchmarks/baseline-1.4.json`. Timing samples came from a developer laptop kept idle during the run, not a dedicated runner. |
| CI | The dispatched W1-Test run at `b725be1` passed every job on Astro 5, 6 and 7 and Node 20.19.5, 22 and 24, including the workerd edge suite and both action jobs. An earlier run exposed a batch-order race in the live crawler, fixed with a deterministic test in the same commit series. |
| SARIF upload | The `audit action real upload` job uploaded `fixtures/dist-valid` findings to GitHub code scanning: analysis `1816741655`, category `astro-aeo`, 3 results and 3 rules at `7887dd6`. Verified through the code-scanning analyses API. |
| Packed size | 350,713 packed and 1,358,848 unpacked bytes across 164 files. Ceilings are 450,000 and 1,380,000 in `benchmarks/thresholds.mjs`. |

## Not verified

- No static edge handler has run on a deployed Cloudflare, Netlify or Vercel project. The Cloudflare
  handler passes the Accept contract in local workerd; Netlify and Vercel pass it against their
  documented request APIs only. The README states this. Canary deployments need provider credentials
  that were not available: the local Wrangler login has expired and no Netlify or Vercel CLI is
  configured.

## External results

Both documents were submitted on 2026-09-22 UTC. The 1.2 fixture results are identical to the 1.3.0
record.

| Tool / entity | Result | Errors and warnings | Evidence |
| --- | --- | --- | --- |
| Schema.org Validator, 1.2 fixture | 8 items: WebPage (2), HowTo, Organization, BreadcrumbList, Article, WebSite, FAQPage | 0 errors, 0 warnings | `.context/release-1.4.0/schema-validator.jpg` |
| Google, 1.2 fixture, Article | 1 valid item | 0 critical; 4 non-critical date warnings (F1) | [Result](https://search.google.com/test/rich-results/result?id=VMNNmqEUQONKt0KAuXMpJw), `rich-results.jpg`, `article-warnings.jpg` |
| Google, 1.2 fixture, BreadcrumbList and Organization | 1 valid item each | No issues reported | Same result |
| Google, 1.2 fixture, FAQPage / HowTo / speakable WebPage / managed graph | Not applicable to the rich-result test | Not scored as passed | Same result |
| Schema.org Validator, TechArticle page | 1 item: TechArticle | 0 errors, 0 warnings | `techarticle-schema-validator.jpg` |
| Google, TechArticle page | 1 valid Article item, eligible | 0 critical; 2 non-critical: optional `image` and optional `author.url` missing (F2) | [Result](https://search.google.com/test/rich-results/result?id=K3_3dtlhpg70cygGLYjQ3w), `techarticle-rich-results.jpg`, `techarticle-warnings.jpg` |

## Validator findings

| ID | Finding | Cause and disposition | Owner / decision |
| --- | --- | --- | --- |
| F1 | Article has four non-critical warnings: invalid datetime and missing timezone for `datePublished` and `dateModified`. | Frozen fixture inputs are `2026-08-11` and `2026-08-12`. Unchanged from 1.3.0; README recommends ISO 8601 datetimes with offsets. | Release owner: pending. |
| F2 | TechArticle has two non-critical notes: optional `image` and optional `author.url` are missing. | The fixture deliberately carries only the fields the Starlight plugin's `techArticle` option emits from frontmatter (headline, description, language, modified date). Nothing is inferred, by design. | Release owner: pending. |

## Release sign-off

- [x] Candidate toolchain and the exact carry-forward basis are recorded.
- [x] Every changed semantic source file is listed with whether the fixture can reach it.
- [x] The submitted fixture HTML is proven byte-identical by rebuild and hash.
- [x] Full local release checks pass, including adapter runtimes, the edge suite and performance ceilings.
- [x] The package-size regressions and the ceiling change are recorded with measurements.
- [x] The 1.2 fixture and a TechArticle page were resubmitted to both validators and the results recorded here.
- [ ] Human reviewer accepts findings F1 and F2 and approves the semantic evidence.
- [x] The 1.4 benchmark reference is recorded and committed; the SARIF upload is verified in code scanning.
- [ ] The full gate is rerun at the final candidate commit and its result recorded above.
- [ ] Overall status is changed to **Passed** after that review, before tagging `1.4.0`.

After the approved commit, rerun the full gate with `--tag 1.4.0 --require-tag --require-clean` and
verify the new commit's CI before tagging. Any subsequent shipped-source change requires reassessment of
this evidence. Passing validation does not guarantee search appearance or indexing.
