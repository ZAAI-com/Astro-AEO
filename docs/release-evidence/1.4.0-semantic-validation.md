# Astro-AEO 1.4.0 semantic validation evidence

Draft. Unlike 1.3.1, 1.4.0 does change JSON-LD-producing source, so a carry-forward cannot simply be
asserted. It is proposed here on a narrower basis, proven below: the submitted fixture HTML is
byte-identical, and each semantic change is unreachable from that fixture. The release owner decides
whether that basis is accepted or the fixture is resubmitted. The external services are manual release
checks, not automated release dependencies.

## Record

| Field | Value |
| --- | --- |
| Release | `1.4.0` |
| Validation date (UTC) | 2026-09-22 |
| Fixture | `fixtures/golden-1.2` |
| Candidate commit | `2e07672` plus the commit that adds this record, the folded changelog and the tightened ceilings |
| Build toolchain | Node 24.15.0, pnpm 11.17.0, Astro 7.3.1 |
| Submission method | Proposed carry-forward from 1.3.0: complete rendered HTML pasted into both validators; no public deployment needed. |
| Submitted HTML SHA-256 | `eb9fae45e534ad8eea7b9523be0a564e389362a37455b0ca42325d556c33cf3c`. Rebuilt from this tree on 2026-09-22 and hashed: identical to the 1.3.0 submission. |
| Operator | Claude, repository verification and local gate execution |
| Human reviewer | Pending: the release owner has not yet reviewed this record. |
| Overall status | Pending |

## Semantic source changed in 1.4.0

`git diff --name-only origin/main...HEAD` lists three paths under the JSON-LD surface.

| Path | Change | Reachable from the submitted fixture |
| --- | --- | --- |
| `src/schema.js` | Adds `createTechArticle()`, one more `buildEntity` wrapper. Renames a JSDoc-local typedef. No existing builder, merge, validation or serialization code changes. | No. The fixture uses no TechArticle. |
| `src/core/head.js` | The inferred site-wide `WebSite` entity omits `inLanguage` when the site declares more than one Astro i18n locale. Repeated `generator` meta tags are exempt from the duplicate-metadata diagnostic. | No. The fixture has one locale, so its `WebSite` bytes are unchanged; the second change affects a diagnostic, not output. |
| `components/index.d.ts` | Adds the optional `version` prop to the `AeoPage` declaration. | No. A type declaration, and `AeoPage` emits no JSON-LD. |

The byte-identical fixture hash above is the proof that these hold for the submitted page, not only
the reasoning in this table. New JSON-LD behavior that no fixture submission covers: a `TechArticle`
entity (from `createTechArticle()` and from the Starlight plugin's `techArticle` option) and the
multilingual `WebSite` entity without `inLanguage`. Both are covered by unit tests only.

## Repository checks

| Check | Method and result |
| --- | --- |
| Frozen outputs | `fixtures/golden-1.0` and `fixtures/golden-1.2` pass byte for byte in the default suite, as does `fixtures/config-compat`. |
| Builder coverage | `src/schema.test.js` covers all 18 builders, including `createTechArticle`; public consumer types pass on TypeScript 5.5. |
| Multilingual WebSite | `src/core/head.test.js` pins the entity with and without a second locale, and `recipes/i18n` builds, which it could not before the fix. |
| Full release gate | `pnpm run release:check` passed at `2e07672` in 140 seconds: schema and migration-doc freshness, Changeset coverage, release metadata, both type checks, the default suite (1768 tests), development (29), SSR (50), the trailing-slash matrix, adapter builds (27) and runtimes on Node, workerd and Deno (108), the workerd edge suite (29), recipes (9), the 10,000-page audit benchmark, bundle baselines, packed-tarball install, the IndexNow transport smoke and performance ceilings. It must be rerun at the final candidate commit. |
| Performance comparison | Two portable regressions, both package size and both explained in `CHANGELOG.md`: 28.76 percent packed and 22.83 percent unpacked against the committed 1.3 baseline. No Node or Cloudflare bundle regression and no absolute ceiling failure. Environment-sensitive metrics were skipped because runner classes differ. |
| Packed size | 350,713 packed and 1,358,848 unpacked bytes across 164 files. Ceilings are 450,000 and 1,380,000 in `benchmarks/thresholds.mjs`. |

## Not verified

- No static edge handler has run on a deployed Cloudflare, Netlify or Vercel project. The Cloudflare
  handler passes the Accept contract in local workerd; Netlify and Vercel pass it against their
  documented request APIs only. The README states this.
- The GitHub Action has not performed a real SARIF upload to GitHub code scanning. Its audit step was
  run locally and its CI smoke job uploads nothing.
- The benchmark baseline is still the 1.3 reference. A 1.4 reference needs the declared reference
  runner.

## External results

Proposed carry-forward, unchanged from `1.3.0-semantic-validation.md`, which remains the authoritative
record of the submissions against byte-identical content.

| Tool / entity | Result | Errors and warnings |
| --- | --- | --- |
| Schema.org Validator, complete page | 8 items detected | 0 errors, 0 warnings |
| Google, Article | 1 valid item | 0 critical errors; 4 non-critical date warnings (F1 in the 1.3.0 record, accepted) |
| Google, BreadcrumbList and Organization | 1 valid item each | No issues reported |
| Google, FAQPage / HowTo / speakable WebPage / managed graph | Not applicable to this fixture's rich-result test | Not scored as passed |

## Release sign-off

- [x] Candidate toolchain and the exact carry-forward basis are recorded.
- [x] Every changed semantic source file is listed with whether the fixture can reach it.
- [x] The submitted fixture HTML is proven byte-identical by rebuild and hash.
- [x] Full local release checks pass, including adapter runtimes, the edge suite and performance ceilings.
- [x] The package-size regressions and the ceiling change are recorded with measurements.
- [ ] Human reviewer accepts the carry-forward, or the fixture is resubmitted and the results recorded here.
- [ ] Human reviewer decides whether a TechArticle or multilingual fixture is submitted before the tag.
- [ ] The full gate is rerun at the final candidate commit and its result recorded above.
- [ ] Overall status is changed to **Passed** after that review, before tagging `1.4.0`.

After the approved commit, rerun the full gate with `--tag 1.4.0 --require-tag --require-clean` and
verify the new commit's CI before tagging. Any subsequent shipped-source change requires reassessment of
this evidence. Passing validation does not guarantee search appearance or indexing.
