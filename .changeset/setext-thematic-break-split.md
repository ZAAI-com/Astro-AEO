---
'astro-aeo': patch
---

Keep a leading thematic break separate from a following setext heading when scanning Markdown blocks, so `---` followed by `Title` and `===` plans the break and heading as distinct blocks instead of one merged heading.
