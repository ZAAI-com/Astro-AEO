---
"astro-aeo": minor
---

Harden 1.3 corpus, IndexNow, robots, sitemap, and runtime contracts: manifests tell the truth for companion-less pages and published `.md` hashes, dependent corpus claims follow ownership, IndexNow enforces `keyLocation` scope and exits 2 on invocation errors, locks reclaim atomically, Content-Signal sits inside each user-agent group, locale-only indexes skip root `/llms.txt` ads, and runtime edges (lifecycle isolation, FetchState, loopback, encoded locales) stay safe.
