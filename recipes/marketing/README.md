# Recipe: marketing

A small marketing site: companions for every page, `llms.txt`, a robots policy for AI crawlers, and an organization graph on the homepage.

```bash
npm install
npm run build
npm run audit
```
