# Astro-AEO 1.2 golden fixture

This fixture freezes the default managed Schema.org graph introduced in 1.2 and the serialized
output of the six pre-existing JSON-LD components. Keep the 1.0 fixture separate: it disables
automatic graph injection to preserve the published 1.0 HTML contract.
