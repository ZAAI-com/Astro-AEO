// @ts-check
// The table ships with the CLI because `astro-aeo doctor --url` probes a deployment
// with it. Tests keep importing it from here, the contract's home.
export { ACCEPT_CONTRACT } from '../../cli/accept-contract.js';
